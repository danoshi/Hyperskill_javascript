// Construct the URL here
const urlObject = {
    protocol: 'https:',
    hostname: 'www.example.com',
    pathname: '/mydocument',
    port: '443',
};

// Constructed URL

const constructedURL = url.format(urlObject);
